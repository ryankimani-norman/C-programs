// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // measurements
    int length;
    int width;
    int area;
    // enter length
    ;printf("Enter a length",length);
    ;scanf("%d",&length);
// enter width
  ;printf("Enter a width",width);
   ;scanf("%d",&width);
   // area is length by width
   area=length*width
  ;printf("/n Area of the rectangle is:%d",area);
    return (0);
}
